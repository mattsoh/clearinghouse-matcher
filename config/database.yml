default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV['DATABASE_URL'] %>

development:
  <<: *default
  database: clearinghouse_matcher_development

test:
  <<: *default
  database: clearinghouse_matcher_test

production:
  <<: *default
  database: clearinghouse_matcher_production
